tenant: acme
flow_type_bindings:
  messaging:
    adapter: telegram
    config:
      public_base_url: "https://<public-ngrok-or-domain>"
      route: "/messaging/telegram/webhook"
    secrets: [ TELEGRAM_BOT_TOKEN ]
rate_limits:
  messaging_send_qps: 10
  messaging_burst: 20
